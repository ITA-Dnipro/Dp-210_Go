package handlers

const incorrectEmailOrPassword = "the email or password was incorrect"
const incorrectEmailOrAuthCode = "the email or authentication code was incorrect"
const invalidRequestFormat = "invalid request format"
const requestFailed = "request failed"
